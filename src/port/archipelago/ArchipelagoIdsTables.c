// Defines the generated name/type tables declared in ArchipelagoIds.h.
// Compiled as C so the const arrays have external linkage. Built in both the
// ENABLE_ARCHIPELAGO=ON and =OFF configurations.
#define AP_IDS_DEFINE_TABLES
#include "ArchipelagoIds.h"
